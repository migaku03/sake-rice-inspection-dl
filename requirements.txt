# Core dependencies actually used by src/ and tests/.
# Trimmed from a raw `pip freeze` (185 packages) down to what the published
# code imports. See README for the reproduction command.
numpy==2.2.1
pandas==2.2.3
scikit-image==0.25.0
matplotlib==3.10.0
Pillow==11.1.0
openpyxl==3.1.5
torch==2.5.1
torchvision==0.20.1
cellpose==4.0.2
opencv-python-headless==4.11.0.86

# Testing
pytest==8.3.4
