"""Sake Rice Quality Inspection: reusable pipeline components.

This package hosts the individually tested building blocks of the sake
rice inspection pipeline (segmentation, preprocessing, domain adaptation,
training utilities). The original end-to-end prototype lives in
`sake_rice_inspection_system.py` at the repository root of `src/`; modules
are being extracted here incrementally as they are refactored for public
release.
"""
